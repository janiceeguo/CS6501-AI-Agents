Agent Orchestration Frameworks:

- Jupyter notebook (all code): `agent_control.ipynb`
- Separate models for each task in each `.py` file
- Separate outputs and graphs for each task in the `outputs` folder