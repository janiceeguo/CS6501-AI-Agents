# Fine-Tuning an LLM

- Jupyter notebook (all code) in `finetune.ipynb`
- Steps 1-3 as the base model and steps 4-7 as the fine-tuned model have separate code in the `.py` files and separate outputs in the `outputs` folder

## Step 6

## Step 7

## Discussion Questions